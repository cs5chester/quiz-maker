import React, {useState} from 'react';
import {useQuizApi} from './hooks/useQuizApi';

function EmailStep({}) {
    return (
        <div className={'quiz-widget__'}>
            email step
        </div>
    )
}

export default EmailStep;